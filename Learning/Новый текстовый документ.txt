SELECT DISTINCT Product.maker 
FROM Product, PC
WHERE Product.model=PC.model AND Product.maker NOT IN (SELECT DISTINCT Product.maker 
FROM Product, Laptop
WHERE Product.model=Laptop.model
)
Использование в запросе нескольких источников записей